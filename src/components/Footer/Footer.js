import React from 'react';
import FilterForm from "../Header/FilterForm/FilterForm";

const Footer = () => {
    return (
        <div>
            <FilterForm/>
        </div>
    );
};

export default Footer;